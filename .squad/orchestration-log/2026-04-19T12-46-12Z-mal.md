# Orchestration: Mal (Lead)

**Date:** 2026-04-19T12:46:12Z  
**Branch:** feature/cli-tool-t2i  
**Status:** Complete

## Work Summary

Scaffolded CLI tool project (`ElBruno.Text2Image.Cli`) as a dotnet tool targeting net10.0 only. Established foundational architecture, interfaces, and DI infrastructure. All stubs implemented; project compiles cleanly.

## Deliverables

- `.csproj` configured as `<OutputType>Exe</OutputType>` with `<PackAsTool>true</PackAsTool>`
- `Program.cs` wired with Spectre.Console.Cli, DI registration via `TypeRegistrar`/`TypeResolver` adapters
- `IProviderAdapter` interface defining contract for local and cloud providers
- `ISecretStore` interface with async get/set/delete/list operations
- `AppConfig` data model for persistent configuration
- DI extension method `AddCliInfrastructure()`
- Updated `.slnx` to include CLI project
- Documentation in `docs/cli-tool.md`
- Build: 0 warnings, 0 errors

## Key Decisions

1. **Spectre.Console.Cli pivot** — Switched from System.CommandLine (preview) to Spectre.Console.Cli (stable, proven in production)
2. **Single TFM (net10.0)** — CLI targets only net10.0 with `<RollForward>LatestMajor</RollForward>` for forward compatibility
3. **Secret store abstraction** — `ISecretStore` interface with multi-backend support (env vars, DPAPI, plaintext file)

## Handoff Status

✅ Ready for parallel work by Wash (infrastructure), Kaylee (commands), River (adapters), Jayne (tests).
